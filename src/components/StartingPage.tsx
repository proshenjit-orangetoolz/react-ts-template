import { Box, Typography } from 'convertupleads-theme';

const StartingPage = () => {
    return (
        <Box alignItems={'center'}>
            <Typography variant={'h1'}>Hello there!</Typography>
        </Box>
    );
};
export default StartingPage;
